{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "zig build -Drelease-small=true; w4 run zig-out/lib/cart.wasm -n",
            "problemMatcher": []
        },
        {
            "label": "build&run",
            "type": "shell",
            "command": "zig build -Drelease-small=true; w4 run zig-out/lib/cart.wasm -n"
        }
    ]
}